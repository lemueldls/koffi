// @generated by koffi

#ifndef {{ schema.crate_ident()|upper }}_H
#define {{ schema.crate_ident()|upper }}_H

#include <stdint.h>

// Fieldless enums arrive as their discriminant C type; the Kotlin side
// casts the value back to a real enum. Data-carrying enums are a wrapper
// struct with an explicit discriminant and a payload union.
{%~ for e in schema.enums %}
{% if e.has_data %}
{%- for v in e.variants %}{% if !v.placements.is_empty() %}
typedef struct {
    {%- for f in v.placements %}
    {{ f.ty.c_type() }} {{ f.name }};
    {%- endfor %}
} {{ e.payload_ident(v) }};
{% endif %}
{%- endfor %}
typedef union {
    {%- for v in e.variants %}{% if !v.placements.is_empty() %}
    {{ e.payload_ident(v) }} {{ e.variant_wire_name(v) }};
    {%- endif %}{%- endfor %}
} {{ e.payload_union_ident() }};

typedef struct {
    {{ e.discriminant.c_type() }} _d;
    {{ e.payload_union_ident() }} _u;
} {{ e.unique_ident() }};
{% else %}
typedef {{ e.discriminant.c_type() }} {{ e.unique_ident() }};
{% endif %}
{%- endfor %}

// repr(C) in Rust means no padding bookkeeping here: the header mirrors
// the struct's field list directly.
{%~ for s in schema.structs_in_layout_order() %}
typedef struct {
    {%- for f in s.fields %}
    {{ f.ty.c_type() }} {{ f.name }};
    {%- endfor %}
} {{ s.unique_ident() }};
{%- endfor %}

{%~ for f in schema.functions %}
{{ f.return_type.c_type() }} {{ f.c_abi_symbol() }}({% for p in f.params %}{{ p.ty.c_type() }} {{ p.c_abi_symbol() }}{% if !loop.last %}, {% endif %}{% else %}void{% endfor %});
{%- endfor %}

#endif
