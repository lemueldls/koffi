// @generated by koffi

#ifndef {{ schema.crate_ident()|upper }}_H
#define {{ schema.crate_ident()|upper }}_H

#include <stdint.h>
#include <stdbool.h>

{%~ for s in schema.structs %}
// typedef struct {
//     TODO: fields, in layout.rs's computed order
// } {{ s.unique_ident() }};
{%- endfor %}

{%~ for f in schema.functions %}
// TODO: {{ f.return_type.unique_ident() }} {{ f.c_abi_symbol() }}(...);
{%- endfor %}

#endif
