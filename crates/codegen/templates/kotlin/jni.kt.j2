// @generated by koffi

package {{ schema.kotlin_package() }}

actual object {{ schema.ffi_object_name() }} {
    init {
        System.loadLibrary("{{ schema.crate_ident() }}_glue")
    }

    {%~ for f in schema.functions %}
    actual external fun {{ f.kotlin_name }}(
        {%- for p in f.params %}
        {{ p.name }}: {{ p.ty.kotlin_type() }}{% if !loop.last %},{% endif %}
        {%- endfor %}
    ): {{ f.return_type.kotlin_type() }}
    {%- endfor %}
}
