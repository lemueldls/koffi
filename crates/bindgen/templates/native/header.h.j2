/* @generated by koffi. do not edit. */

#ifndef {{ guard }}
#define {{ guard }}

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

typedef struct {
    uint8_t* ptr;
    size_t   len;
    size_t   cap;
} KoffiByteBuf;

extern void koffi_handle_release(uint64_t handle_id);
extern void koffi_free_byte_buf(KoffiByteBuf buf);

{%- for f in ir.functions %}
extern {{ f.ret_ty | c_type }} {{ f.rust_name }}(
    {%- if f.parent_struct.is_some() %}
    uint64_t handle_id{% if !f.params.is_empty() %},{%endif%}
    {%- endif %}
    {%- for p in f.params %}
    {{ p.ty | c_type }} {{ p.name }}{% if p.ty.needs_len_param() %},
    size_t {{ p.name }}_len{% endif %}{% if !loop.last %},{% endif %}
    {%- endfor %}
);
{% endfor %}

#endif /* {{ guard }} */
